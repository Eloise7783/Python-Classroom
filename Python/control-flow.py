# Sequence - order of operations
# Selection - Deciding what step to run after a certain event
# Iteration - running over the same thing many times

inputString = input("input a word ");

if 'a' in inputString:
    print ('your input contained an a')
else:
    print ('your input did not contain an a')

number = int(input('input a number'))
if number > 0 and number < 50:
    print ('you win!')
